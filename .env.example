# ── Public (shipped to browser — safe) ─────────────────────────────
PUBLIC_BRAND=veralify
PUBLIC_PRIVY_APP_ID=
PUBLIC_TWENTY_URL=
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Canny feedback board (Settings → board → Install)
PUBLIC_CANNY_SUBDOMAIN=
PUBLIC_CANNY_BOARD_TOKEN=

# ── Server-only secrets (NEVER prefix with PUBLIC_, never commit) ───
# Supabase Dashboard → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=

# Resend → https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxx
# Use a verified domain in production (e.g. noreply@veralify.com).
# onboarding@resend.dev only delivers to your own Resend account email.
RESEND_FROM=onboarding@resend.dev
